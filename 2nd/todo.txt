1.「SQLStatement_create_1.sql」を実行し、create文の説明をする。

2.「SQLStatement_create_2.sql」を実行してなぜエラーになるか考えてもらう。(意外と嵌りやすい罠)

2-1.生成AIに以下の内容を投げる。
```
CREATE TABLE members
	(member_id		NUMBER(4) CONSTRAINT pk_mem PRIMARY KEY,
	member_name		VARCHAR2(60),
	qualification_id		NUMBER(4));
CREATE TABLE qualifications
	(qualification_id		NUMBER(4) CONSTRAINT pk_qualifi PRIMARY KEY,
	qualification_name		VARCHAR2(60));
CREATE TABLE members_sub
	(member_id		NUMBER(4) CONSTRAINT pk_mem PRIMARY KEY,
	member_name		VARCHAR2(60),
	qualification_id		NUMBER(4));
Oracle DBで上記を実行したらエラーになったんだけど原因を教えて
```

2-2.member_subのCONSTRAINTを削除して実行する。
2-3.Schemaから作成したテーブルのConstraintを確認する。(スキーマから確認できる)

3.「SQLStatement_left_join_etc.sql」を実行し、結合について確認する。

4.Ping -tでSelect文の問題をみんなで解いて解説する